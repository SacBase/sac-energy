noinline
int test()
{
    return Array::sum(Array::iota([1000,1000,100]));
}

int main()
{
    Rapl::rapl_reset();

    StdIO::print(test());

    elapsed = Rapl::rapl_elapsed();
    StdIO::print(elapsed);

    return 0;
}
